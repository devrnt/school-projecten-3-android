package com.talentcoach.id11.id11_android.models
/**
 * Deel van *models*.
 *
 * Specifieke Info => Nog niet uitgerwerkt, niet in de applicatie
 * @property id het id van de specifieke info
 * @property titel de titel van de specifieke info
 * @property omschrijving de omschrijving van de specifieke info
 * @property werkgever de bijhorende werkgever van wie de info komt
 * @constructor Maakt een nieuw SpecifiekeInfo object
 */
class SpecifiekeInfo(val id: Int, val titel: String, val omschrijving: String, val werkgever: Werkgever)